// @flow
import { StyleSheet, PixelRatio } from "react-native";
import { Metrics, Colors } from "../../../theme";

export default StyleSheet.create({
  completedDriverAddress: {
    height: 92,
    alignItems: "center"
  }
});
